while it did not find extensive use it introduced many features that are used widely now and is seen as a step in the development of object oriented programming oop key contributions include abstract data types call by sharing iterators multiple return values a form of parallel assignment type safe parameterized types and type safe variant types it is also notable for its use of classes with constructors and methods but without inheritance the syntax of clu was based on algol then the starting point for most new language designs the key addition was the concept of a cluster clu s type extension system and the root of the language s name cluster clusters correspond generally to the concept of a class in an oo language and have similar syntax for instance here is the clu syntax for a cluster that implements complex numbers a cluster is a module that encapsulates all of its components except for those explicitly named in the is clause these correspond to the public components of a class in recent oo languages a cluster also defines a type that can be named outside the cluster in this case complex number but its representation type rep