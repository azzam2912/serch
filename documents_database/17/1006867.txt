it tries to make the net types equivalent to the com types in addition com interop allows com developers to access managed objects as easily as they access other com objects the net framework creates a type library and special registry entries when a component is registered it provides a specialized utility codice 1 usually located in codice 2 that exports the managed types into a type library and registers the managed component as a traditional com component when the type is instantiated through com the net clr is the actual com object that executes and it merely marshals any method calls or property access to the type implementation registration free com can be used to avoid dll hell a com interop for an existing com component can be created through the use of the codice 3 tool supplied with the net framework sdk this tool will create a set of proxy classes implemented in cil which is encapsulated in an assembly com interop automatically provides the following services to simplify com usage from net