the entity referred to by these keywords thus depends on the execution context such as which object is having its method called different programming languages use these keywords in slightly different ways in languages where a keyword like this is mandatory the keyword is the only way to access data and methods stored in the current object where optional they can disambiguate variables and functions with the same name in many object oriented programming languages codice 1 also called codice 2 or codice 3 is a variable that is used in instance methods to refer to the object on which they are working c and languages which derive in style from it such as java c d and php generally use codice 1 smalltalk and others such as object pascal perl python ruby rust objective c dataflex and swift use codice 2 microsoft s visual basic uses codice 3 the concept is similar in all languages codice 1 is usually an immutable reference or pointer which refers to the current object the current object often being the code that acts as parent to the property method sub routine or function that contains the codice 1 keyword after an object is